% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/library.R
\name{mk_library_songs}
\alias{mk_library_songs}
\title{Fetch songs in the user's library}
\usage{
mk_library_songs(limit = 100, creds = mk_credentials())
}
\arguments{
\item{limit}{Max songs to fetch (Apple's ceiling is 100 per page).}

\item{creds}{A credentials list from \code{\link[=mk_credentials]{mk_credentials()}}, including a
\code{user_token} (see \code{\link[=require_user_token]{require_user_token()}}).}
}
\value{
A tibble, same shape as \code{\link[=mk_song]{mk_song()}}'s return value.
}
\description{
Wraps \code{GET /v1/me/library/songs}. Library songs carry the same
shape as catalog songs (name, artistName, genreNames, ...) but no
\code{url}, since they're personal library entries rather than public
catalog pages -- that column comes back \code{NA}.
}
\details{
Apple caps this endpoint at 100 items per request; this wraps a
single page rather than following pagination, so a library larger
than \code{limit} will be truncated. Fine for building a taste profile
(which just needs a representative sample), not a full library
export.
}
\examples{
\dontrun{
mk_library_songs(limit = 50)
}
}
