% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend.R
\name{mk_artist_top_songs}
\alias{mk_artist_top_songs}
\title{Fetch an artist's top songs}
\usage{
mk_artist_top_songs(
  id,
  limit = 10,
  storefront = NULL,
  creds = mk_credentials()
)
}
\arguments{
\item{id}{Catalog artist ID (e.g. from \code{\link[=mk_search]{mk_search()}}'s \code{id} column).}

\item{limit}{Max songs to fetch.}

\item{storefront}{Storefront code, e.g. \code{"us"}. Defaults to
\code{creds$storefront}.}

\item{creds}{A credentials list from \code{\link[=mk_credentials]{mk_credentials()}}.}
}
\value{
A tibble, same shape as \code{\link[=mk_song]{mk_song()}}'s return value.
}
\description{
Wraps the Apple Music "top songs" view
(\verb{/v1/catalog/\{storefront\}/artists/\{id\}/view/top-songs}) -- songs
associated with the artist based on popularity in the current
storefront, the closest thing to a "start here" list for an artist
since there's no numeric popularity score to sort by directly.
}
\examples{
\dontrun{
mk_artist_top_songs("623897863") # Bad Suns
}
}
