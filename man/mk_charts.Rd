% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts.R
\name{mk_charts}
\alias{mk_charts}
\title{Fetch Apple Music charts}
\usage{
mk_charts(
  types = c("songs", "albums"),
  storefront = NULL,
  genre = NULL,
  limit = 20,
  creds = mk_credentials()
)
}
\arguments{
\item{types}{Character vector of chart types: \code{"songs"}, \code{"albums"},
or \code{"music-videos"}. Note: Apple does not offer an \code{"artists"}
chart type.}

\item{storefront}{Storefront code, e.g. \code{"us"}. Defaults to
\code{creds$storefront}.}

\item{genre}{Optional genre ID to scope the chart to.}

\item{limit}{Max entries per chart (Apple's ceiling is 200, paginated
in pages of up to 50).}

\item{creds}{A credentials list from \code{\link[=mk_credentials]{mk_credentials()}}.}
}
\value{
A named list of tibbles, one per requested chart type, each
with a \code{rank} column.
}
\description{
Chart position is the closest thing the Apple Music API offers to a
popularity signal (there is no numeric popularity score on artist or
song resources) -- so tidied chart results include a \code{rank} column,
1-indexed by array order.
}
\examples{
\dontrun{
mk_charts(types = "songs", limit = 50)
}
}
