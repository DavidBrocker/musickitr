% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/library.R
\name{mk_recently_played_tracks}
\alias{mk_recently_played_tracks}
\title{Fetch the user's recently played tracks}
\usage{
mk_recently_played_tracks(
  limit = 30,
  types = c("songs", "library-songs"),
  creds = mk_credentials()
)
}
\arguments{
\item{limit}{Max tracks to fetch (Apple's ceiling is 30).}

\item{types}{Character vector of track types to include, from
\code{"songs"}, \code{"library-songs"}, \code{"music-videos"},
\code{"library-music-videos"}.}

\item{creds}{A credentials list from \code{\link[=mk_credentials]{mk_credentials()}}, including a
\code{user_token}.}
}
\value{
A tibble, same shape as \code{\link[=mk_song]{mk_song()}}'s return value.
}
\description{
Wraps \code{GET /v1/me/recent/played/tracks} -- note this is a different,
narrower endpoint than \verb{/v1/me/recent/played} (which returns albums,
playlists, and stations, not individual tracks). \code{types} is required
by the endpoint; defaults to catalog and library songs.
}
\examples{
\dontrun{
mk_recently_played_tracks(limit = 30)
}
}
