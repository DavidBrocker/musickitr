% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend.R
\name{mk_recommend_songs}
\alias{mk_recommend_songs}
\title{Recommend songs based on a listener's taste profile}
\usage{
mk_recommend_songs(
  profile = NULL,
  n_seed_artists = 5,
  n_similar_per_artist = 5,
  n_top_genres = 15,
  n_candidate_artists = 10,
  n_songs_per_artist = 5,
  storefront = NULL,
  delay = 0.1,
  creds = mk_credentials()
)
}
\arguments{
\item{profile}{A taste profile from \code{\link[=mk_taste_profile]{mk_taste_profile()}}. Built fresh
if omitted.}

\item{n_seed_artists}{How many of the profile's top artists to expand
outward from.}

\item{n_similar_per_artist}{Max similar artists to pull per seed
(Apple returns up to 10).}

\item{n_top_genres}{How many of the profile's top genres to score
candidates against (keeps scoring focused on the listener's actual
center of gravity rather than long-tail one-off genres).}

\item{n_candidate_artists}{How many top-scoring candidate artists to
pull songs from.}

\item{n_songs_per_artist}{Max songs to pull per candidate artist.}

\item{storefront}{Storefront code, e.g. \code{"us"}. Defaults to
\code{creds$storefront}.}

\item{delay}{Seconds to pause between API calls (see
\code{\link[=mk_similarity_graph]{mk_similarity_graph()}}).}

\item{creds}{A credentials list from \code{\link[=mk_credentials]{mk_credentials()}}, including a
\code{user_token} (needed by \code{\link[=mk_taste_profile]{mk_taste_profile()}} if \code{profile} isn't
supplied).}
}
\value{
A tibble of recommended songs (same columns as \code{\link[=mk_song]{mk_song()}}),
plus \code{source_artist} (which candidate artist it came from) and
\code{score} (that artist's genre-overlap score against the profile),
sorted by score descending.
}
\description{
The pipeline this stitches together, end to end: take the listener's
most-played artists from \code{\link[=mk_taste_profile]{mk_taste_profile()}}, resolve each to a
catalog ID via \code{\link[=mk_search]{mk_search()}} (library/history endpoints only return
artist \emph{names}, not catalog IDs, unless you separately request the
\code{artists} relationship), expand each one hop outward with
\code{\link[=mk_similar_artists]{mk_similar_artists()}} -- the same seed/hop mental model
\code{\link[=mk_similarity_graph]{mk_similarity_graph()}} uses -- then score every candidate artist
surfaced that way by genre overlap with the profile via \code{\link[=mk_jaccard]{mk_jaccard()}}.
Candidates surfaced by more than one seed artist (the graph's
"convergence points") get a small bonus, on the theory that an artist
your library reaches from multiple directions is a stronger signal
than one reached from a single artist. The top-scoring candidate
artists' top songs (\code{\link[=mk_artist_top_songs]{mk_artist_top_songs()}}) are the final
recommendations.
}
\details{
Seed artists already present in the taste profile are excluded from
the candidate pool, so this surfaces new artists rather than just
echoing songs already in the library.
}
\examples{
\dontrun{
mk_recommend_songs()
}
}
