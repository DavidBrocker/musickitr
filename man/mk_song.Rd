% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/songs.R
\name{mk_song}
\alias{mk_song}
\title{Fetch a single song by catalog ID}
\usage{
mk_song(id, storefront = NULL, creds = mk_credentials())
}
\arguments{
\item{id}{Catalog song ID (e.g. from \code{\link[=mk_search]{mk_search()}}'s \code{id} column).}

\item{storefront}{Storefront code, e.g. \code{"us"}. Defaults to
\code{creds$storefront}.}

\item{creds}{A credentials list from \code{\link[=mk_credentials]{mk_credentials()}}.}
}
\value{
A one-row tibble, same shape as the \code{songs} tibble returned
by \code{\link[=mk_search]{mk_search()}} (id, name, artist_name, album_name, duration_ms,
release_date, genres, url, preview_url). \code{preview_url} (a 30-second
\code{.m4a} clip) feeds \code{\link[=mk_play_preview]{mk_play_preview()}}; it's \code{NA} for songs that
don't have one.
}
\description{
Fetch a single song by catalog ID
}
\examples{
\dontrun{
mk_song("1516773561") # "Molecules" - Aesop Rock
}
}
