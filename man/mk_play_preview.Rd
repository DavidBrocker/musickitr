% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/playback.R
\name{mk_play_preview}
\alias{mk_play_preview}
\title{Play a song's preview clip, with a confirmation prompt}
\usage{
mk_play_preview(song, ask = TRUE)
}
\arguments{
\item{song}{A one-row tibble with \code{name}, \code{artist_name},
\code{album_name}, and \code{preview_url} columns -- i.e. a single row from
\code{\link[=mk_song]{mk_song()}}, \code{\link[=mk_search]{mk_search()}}'s \code{songs} tibble, \code{\link[=mk_recommend_songs]{mk_recommend_songs()}},
or anything else built on \code{\link[=mk_tidy_songs]{mk_tidy_songs()}} (which is everything
song-shaped in this package).}

\item{ask}{Whether to prompt for confirmation before playing. Defaults
to \code{TRUE}, so nothing plays without you saying yes first. If R
isn't running interactively (\code{Rscript}, a knitted document), there's
no one to answer the prompt, so playback is skipped entirely rather
than silently proceeding.}
}
\value{
Invisibly, \code{TRUE} if playback happened, \code{FALSE} if it was
skipped (declined at the prompt, skipped non-interactively, or
\code{ask = FALSE} was never actually reached because there's no preview
available).
}
\description{
Neither R nor RStudio has anything resembling a native audio engine --
base R can't decode or play audio at all. Apple Music previews are
\code{.m4a} (AAC) files, which rules out most audio-adjacent CRAN packages
as a clean fit (\pkg{tuneR}/\pkg{audio} only handle WAV; \pkg{av} can
decode AAC but doesn't play it back). The practical fix is shelling
out to \code{afplay}, the command-line player already built into macOS,
which handles \code{.m4a} natively. \code{afplay} given the URL directly is
unreliable, though -- some of Apple's CDN redirects trip it up -- so
this downloads the preview to a temporary file first (via
\pkg{httr2}, already a dependency) and plays that instead, deleting
it afterward. This is macOS-only by construction; there's no
cross-platform fallback (yet).
}
\examples{
\dontrun{
song <- mk_song("1516773561") # "Molecules" - Aesop Rock
mk_play_preview(song)
}
}
