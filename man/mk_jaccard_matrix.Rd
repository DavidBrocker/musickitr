% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/similarity.R
\name{mk_jaccard_matrix}
\alias{mk_jaccard_matrix}
\title{Pairwise Jaccard similarity matrix across a tidy tibble}
\usage{
mk_jaccard_matrix(data, id_col = "name", set_col = "genres", delim = ", ")
}
\arguments{
\item{data}{A tibble with an id column and a delimited-string column.}

\item{id_col}{Column of labels to compare (e.g. artist name).}

\item{set_col}{Column of delimiter-joined values (e.g. genres).}

\item{delim}{Delimiter used within \code{set_col}. Must match whatever
\code{\link[=mk_collapse]{mk_collapse()}} (or your own tidying) used to join the values.}
}
\value{
A tibble with columns \code{item_a}, \code{item_b}, \code{similarity} -- one
row per unique pair.
}
\description{
Takes a tibble like the one returned by \code{\link[=mk_search]{mk_search()}} or \code{\link[=mk_charts]{mk_charts()}}
-- with an identifying column (e.g. \code{name}) and a delimited-string
column (e.g. \code{genres}, as produced by \code{\link[=mk_collapse]{mk_collapse()}}) -- and returns
every pairwise similarity as a long tibble, ready to feed into a
network/graph visualization (e.g. edge weights between star nodes).
}
\examples{
\dontrun{
artists <- mk_search("indie rock", types = "artists")$artists
mk_jaccard_matrix(artists, id_col = "name", set_col = "genres")
}
}
