% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/library.R
\name{mk_heavy_rotation}
\alias{mk_heavy_rotation}
\title{Fetch the user's heavy rotation content}
\usage{
mk_heavy_rotation(limit = 10, creds = mk_credentials())
}
\arguments{
\item{limit}{Max entries to fetch.}

\item{creds}{A credentials list from \code{\link[=mk_credentials]{mk_credentials()}}, including a
\code{user_token}.}
}
\value{
A tibble, same shape as \code{\link[=mk_song]{mk_song()}}'s return value (some rows
may have \code{NA} in song-specific columns like \code{album_name} if the
underlying resource was an album/playlist/station).
}
\description{
Wraps \code{GET /v1/me/history/heavy-rotation}. Unlike \code{\link[=mk_library_songs]{mk_library_songs()}},
this endpoint returns a mix of resource types -- songs, albums,
playlists, stations -- whatever Apple considers "in heavy rotation"
right now. Reuses \code{\link[=mk_tidy_songs]{mk_tidy_songs()}} as the tidier the same way
\code{\link[=mk_charts]{mk_charts()}} does for music videos: it degrades gracefully (\code{NA} for
whatever attributes a given type doesn't have), which is exactly what
a genre/artist taste signal needs -- playlists and stations will come
back with \code{NA} genres/artist_name and drop out of \code{\link[=mk_taste_profile]{mk_taste_profile()}}
naturally.
}
\examples{
\dontrun{
mk_heavy_rotation(limit = 10)
}
}
